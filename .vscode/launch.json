{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "🖥️ 服务端 (Server)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/1szt/main",
            "args": ["-mode", "server"],
            "cwd": "${workspaceFolder}"
        },
        {
            "name": "💻 客户端 (Client)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/1szt/main",
            "args": ["-mode", "client"],
            "cwd": "${workspaceFolder}"
        }
    ]
}